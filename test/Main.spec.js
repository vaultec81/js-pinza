const {
    config,
    testAPIs,
    startIpfs,
    stopIpfs,
    implementations
} = require('orbit-db-test-utils');

//TODO create full test boilerplate and tests
describe("Main", () => {
    it("Fire up", (done) => {
        
        done();
    })
})